<div class="mx-auto md:w-2/3 w-full">
    <h1 class="font-bold text-4xl">Edit Recipe Rating</h1>

    <%= render "form", rating: @rating, url: recipe_rating_path(@recipe, @rating) %>

    <%= link_to "Back to recipe", recipe_path(@recipe.id), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>