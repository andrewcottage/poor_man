
<div class="mx-auto max-w-2xl px-4 py-2 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8" id="reviews">
  <h2 class="text-lg font-medium text-gray-900">Recent reviews</h2>
  <%= link_to "Add Yours", "#" %>
  <div class="mt-6 space-y-10 divide-y divide-gray-200 border-b border-t border-gray-200 pb-10">
    <% ratings.each do |rating| %>
      <div class="pt-10 lg:grid lg:grid-cols-12 lg:gap-x-8">
        <div class="lg:col-span-8 lg:col-start-5 xl:col-span-9 xl:col-start-4 xl:grid xl:grid-cols-3 xl:items-start xl:gap-x-8">
          <div class="flex items-center xl:col-span-1">
            <%= render "recipes/recipe/stars", rating: rating.value %>
            <p class="ml-3 text-sm text-gray-700"><%= rating.value %><span class="sr-only"> out of 5 stars</span></p>
          </div>

          <div class="mt-4 lg:mt-6 xl:col-span-2 xl:mt-0">
            <h3 class="text-sm font-medium text-gray-900">TEST</h3>

            <div class="mt-3 space-y-6 text-sm text-gray-500">
              <p>
                rating.comment
              </p>
            </div>
          </div>
        </div>

        <div class="mt-6 flex items-center text-sm lg:col-span-4 lg:col-start-1 lg:row-start-1 lg:mt-0 lg:flex-col lg:items-start xl:col-span-3">
          <p class="font-medium text-gray-900"><%= rating.user.username %></p>
          <time datetime="2021-01-06" class="ml-4 border-l border-gray-200 pl-4 text-gray-500 lg:ml-0 lg:mt-2 lg:border-0 lg:pl-0"><%= rating.created_at.strftime("%B %d, %Y") %></time>
        </div>
      </div>
    <% end %>
  </div>
</div>
